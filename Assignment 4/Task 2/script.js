// Write an if condition to check that age is between 14 and 90 inclusively. “Inclusively” means 
// that age can reach the edges 14 or 90.

let Age = Number(prompt("Enter your age"));

if ( Age >= 19 && Age >=90) {
    console.log("Age is within the limit");
} else {
    console.log("Age is not within the limit");
}